package com.scaler.datastructures.intermediate.arrays.bitmanipulation

object BinaryAddition extends App {
var carry = 0
  var result = 0
  val a = "1010"
  val b = "11"
  var iterations = if (a.length > b.length) a.length else b.length

  while(iterations-1 >= 0) {
    println(iterations)
    iterations = iterations - 1
  }

}
